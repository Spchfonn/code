#include<bits/stdc++.h>
using namespace std;
priority_queue<pair<int, int>> pq;
vector<int> q;
int main(){
    int n, m, x, ans;
    cin >> n;
    while(n--){
        cin >> m >> x;
        if(m==1){
            if(find(q.begin(), q.end(), x)!=q.end()){
                
            }
        }
    }
    
    return 0;
}